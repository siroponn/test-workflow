name: manual
on: 
  workflow_dispatch:
    # inputs:
    #   greeting:
    #     type: string
    #     default: hello
    #     required: true
    #     description: A ch world
    #   loglevel:
    #     description: choise test
    #     type: choice
    #     options:
    #       - info
    #       - warn
    #       - error
jobs:
  run:
    uses: siroponn/test-workflow-call/.github/workflows/callable.yaml@v1
    #runs-on: ubuntu-latest
      #- run: echo "aaaa"

